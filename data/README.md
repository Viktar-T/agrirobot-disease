# data/ — local data root (git-ignored)

Everything in this directory except this file is ignored by git: the open bean sets (downloaded in W1, H8 §6.3; plan ~100–150 GB), feature caches, MCAP bags. Versioning (DVC) and layout are set up in W2 (piece 3); until then nothing here is tracked.
